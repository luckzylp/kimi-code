---
"@moonshot-ai/kimi-code": minor
---

Switch the ACP server to the new adapter, which forwards unknown slash commands to the model as regular prompt text instead of rejecting them, and serves session files under the agent's home directory from the local filesystem.
