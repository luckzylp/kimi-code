---
"@moonshot-ai/kimi-code": minor
---

Report the session's current context window usage (used/total tokens) to ACP clients via usage_update notifications after session setup, model switches, compaction, and each prompt turn, so connected editors can show a context indicator and warn before the context fills up.
